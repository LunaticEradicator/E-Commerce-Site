import { useState } from "react";
import FormContainer from "../components/Reuseable/FormContainer";

export default function ShippingScreen() {
  return <FormContainer>Shipping Address</FormContainer>;
}
